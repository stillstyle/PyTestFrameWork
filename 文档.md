selenium UI测试项目
目标是对126邮箱 进行登录、添加联系人、发送邮件三大功能的自动化测试。（https://mail.126.com/）

准备
1. 安装依赖
pip install pytest selenium pytest-html openpyxl pywin32 yagmail webdriver-manager
2. 修改 config/config.ini
将 [126LoginAccount] 下的 username 和 password 改为你自己的126邮箱账号密码。

运行
确保已修改 config/config.ini 中的账号密码
在 data/tcData.xlsx 中填入测试数据
在项目根目录下执行：
python RunTestCase.py
或在命令行直接运行 pytest：
pytest --html=report.html --self-contained-html
查看报告：执行后会在 report/ 目录下生成带截图和描述的 HTML 测试报告，并自动发送到配置的邮箱。

目录结构：
PytestAutoTestFrameWork/
│
├── config/
│   ├── __init__.py
│   ├── conf.py                 # 全局配置（路径、邮件、报告）
│   └── config.ini              # UI元素定位器 + 测试账号
│
├── data/
│   ├── __init__.py
│   └── tcData.xlsx             # Excel测试数据（登录/联系人/邮件）
│
├── Page/
│   ├── __init__.py
│   ├── BasePage.py             # WebDriver二次封装
│   ├── PageObject/
│   │   ├── __init__.py
│   │   ├── LoginPage.py        # 登录页面对象
│   │   ├── HomePage.py         # 首页菜单
│   │   ├── ContactPage.py      # 添加联系人页面
│   │   └── SendMailPage.py     # 发送邮件页面
│
├── TestCases/
│   ├── __init__.py
│   ├── conftest.py             # 用例级fixture（登录前置）
│   ├── test_loginCase.py       # 登录测试用例
│   ├── test_contactCase.py     # 添加联系人测试用例
│   └── test_sendMailCase.py    # 发送邮件测试用例
│
├── util/
│   ├── __init__.py
│   ├── clipboard.py            # 剪贴板操作
│   ├── keyboard.py             # 模拟键盘按键
│   ├── parseConFile.py         # 解析config.ini
│   ├── parseExcelFile.py       # 解析Excel测试数据
│   └── sendMailForReprot.py    # 发送测试报告邮件
│
├── report/                     # 测试报告输出目录（自动生成）
├── conftest.py                 # 全局fixture（浏览器驱动+截图）
├── pytest.ini                  # pytest配置文件
├── RunTestCase.py              # 一键执行所有用例
└── README.md                   # 项目说明



浏览器驱动：代码使用 Chrome，请确保 Chrome 浏览器已安装，且 webdriver-manager 会自动下载匹配的驱动。
仅支持 Windows：clipboard.py 和 keyboard.py 依赖 pywin32，仅在 Windows 下有效。
126邮箱安全限制：频繁登录可能触发验证码，建议在测试环境中适当增加等待时间或手动处理一次验证。
附件路径：测试数据中的 PFA 列需填写文件的绝对路径。
